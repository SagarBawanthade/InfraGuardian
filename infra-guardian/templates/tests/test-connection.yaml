apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infra-guardian.fullname" . }}-test-connection"
  labels:
    {{- include "infra-guardian.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infra-guardian.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
